{
  "no_curl": {
    "patterns": ["^curl ", "^curl$", "^wget ", "^wget$"],
    "message": "curlを使うな。wgetを使うな。MCPを使え。使えないなら報告しろ"
  },
  "no_git_add_all": {
    "patterns": ["^git add -A", "^git add -a", "^git add --all", "^git add \\.$"],
    "message": "git add -A は禁止。必ずファイルを確認してから個別に add すること。git add -p や git add <file> を使え"
  }
}
